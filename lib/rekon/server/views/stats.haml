%h1 Riak client connected to <em>http://#{Ripple.client.host}:#{Ripple.client.port}/stats</em>

%table
  %tbody
    - @stats.each_pair do |key, value|
      %tr
        %th= key
        %td= String === value ? value : value.inspect

